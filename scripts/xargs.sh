# 将当前文件夹下的.jpg文件重命名为.bak
# 这里的-print0是告诉find命令，在每个输出后面以'\0'作为结束。-0是告诉xargs，使用'\0'来分隔输入，而不是空白符。
# 其中的-I {}是告诉xargs，后面的命令中，用{}表示占位符，将会被实际的参数替代。这样就行了。
# 
find . -name "*.jpg" -print0 | xargs -0 -I {} mv {} {}.bak

